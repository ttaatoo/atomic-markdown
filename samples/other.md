# Other note

This file exists so the relative link in `welcome.md` has a same-workspace target.

Open it with the default editor or with Atomic Markdown — your choice. The extension does not take over double-click.

## What to try from here

- Follow the [welcome sample](./welcome.md) back (same-workspace `.md` via `vscode.open`).
- Confirm an [external link](https://github.com/ttaatoo/atomic-markdown) leaves the workbench.
- Toggle a task without changing anything else in the file:

- [ ] Arrived from welcome.md
- [x] Still just a markdown file on disk
